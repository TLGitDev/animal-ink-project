{
  "name": "animal-ink-project",
  "version": "1.0.0",
  "description": "Monorepo microfrontend: Next.js (shell) + MFE React+Vite (lecteur de musique) exposé en Web Components.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:shell\" \"npm run dev:music-player\" \"npm run dev:fasting-tracker\"",
    "dev:shell": "cd apps/shell && npm run dev",
    "dev:music-player": "cd apps/mfe-music-player && npm run dev",
    "dev:fasting-tracker": "cd apps/mfe-fasting-tracker && npm run dev",
    "build": "npm run build:ui && npm run build:shell && npm run build:music-player",
    "build:ui": "cd packages/ui && (npm ci --include=dev || npm install) && npm run build",
    "build:shell": "cd apps/shell && npm run build",
    "build:music-player": "cd apps/mfe-music-player && npm run build",
    "build:vercel": "npm run build:ui && cd apps/mfe-music-player && (npm ci --include=dev || npm install) && npm run build && mkdir -p ../shell/public/mfe && cp dist/music-player.es.js ../shell/public/mfe/music-player.es.js && cd ../shell && (npm ci --include=dev || npm install) && npm run build",
    "install:all": "npm install && cd apps/shell && npm install && cd ../mfe-music-player && npm install && cd ../../packages/ui && npm install",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules apps/*/dist apps/*/.next packages/*/dist",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "jest",
    "test:e2e": "cypress run",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "cypress:open": "cypress open"
  },
  "keywords": [
    "micro-frontend",
    "next.js",
    "react",
    "typescript",
    "music-player"
  ],
  "author": "Animal Ink",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@babel/plugin-transform-runtime": "^7.28.3",
    "@babel/preset-env": "^7.28.3",
    "@babel/preset-react": "^7.27.1",
    "@babel/preset-typescript": "^7.27.1",
    "@testing-library/jest-dom": "^6.6.4",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "@types/jest": "^29.5.12",
    "babel-jest": "^29.7.0",
    "concurrently": "^9.2.0",
    "cypress": "^13.6.6",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "ts-jest": "^29.4.1"
  }
}
