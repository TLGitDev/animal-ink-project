import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Animal Ink - Plateforme Micro-Frontend",
  description:
    "Une plateforme moderne construite avec des micro-frontends, combinant Next.js et React pour une expérience utilisateur exceptionnelle.",
  keywords: [
    "micro-frontend",
    "next.js",
    "react",
    "typescript",
    "music player",
  ],
  authors: [{ name: "Animal Ink" }],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen flex flex-col`}
      >
        {children}
      </body>
    </html>
  );
}
